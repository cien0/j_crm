<h1>Listing Status</h1>
</br>
<div class="digg_pagination">
<%= will_paginate @status %>
</div>
</br>
<table class="table table-bordered table-striped table-hover">
  <thead>
    <tr>
      <th>id</th>
      <th>kontrahenci id</th>
      <th>status</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @status.each do |status| %>
      <tr>
        <td><%= status.id %></td>
        <td><%= status.nazwa %></td>
        <td><%= status.status_text.status_text %></td>
        <td><%= link_to 'Show', status %></td>
        <td><%= link_to 'Edit', edit_status_path(status) %></td>
        <td><%= link_to 'Destroy', status, method: :delete, data: { confirm: 'Jesteś pewny?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Status', new_status_path %>
</br>
</br>
<div class="digg_pagination">
<%= will_paginate @status %>
</div>